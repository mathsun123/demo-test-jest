const myFunctions = {
  sum: (num1, num2) => num1 + num2,
  checkResult: (x) => x,
  isUndefined: () => undefined,
  createCarList: () => {
    const carList = { engine: true };
    carList["tire"] = false;
    return carList;
  },
};

module.exports = myFunctions;
